<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
	<comment>board-sql.xml</comment>

	<!-- 메인로고 사진 조회 -->
	<entry key="selectMainPhotoList">
		SELECT CONTENT_PATH, BOARD_CONTENT
		FROM PHOTO
		JOIN BOARD
		USING(BOARD_NO)
		WHERE BOARD_NO IN (69, 70, 71)
	</entry>

	<!-- 메인페이지 요일별 애니리스트 조회 -->
	<entry key="getDayList">
		SELECT CONTENT_PATH, BOARD_TITLE
		FROM PHOTO
		JOIN BOARD
		USING(BOARD_NO)
		WHERE BOARD_CD = '5'
	</entry>

	<!-- 이미지, 모달창 동영상 가져오기 -->
	<entry key="selectModalList">
		SELECT BOARD_TITLE, GENRE, CONTENT_PATH, VIDEO_PATH,
		BOARD_CONTENT,
		STORY, DRAWING, RELEASE_DT, AGE
		FROM BOARD
		JOIN PHOTO
		USING(BOARD_NO)
		WHERE
		GENRE IS NOT NULL
		AND TYPE = 'V'
	</entry>
	<!-- 명장면 동영상 조회 -->
	<entry key="selectVideoList">
		SELECT BOARD_TITLE, VIDEO_PATH
		FROM PHOTO
		JOIN BOARD
		USING (BOARD_NO)
		WHERE BOARD_NO IN (63, 64, 65, 66, 67, 68)
	</entry>

	<!-- 신작 에니메이션 조회 -->
	<entry key="selectNewAnimeList">
		SELECT BOARD_TITLE, BOARD_CONTENT, CONTENT_PATH
		FROM
		BOARD
		JOIN PHOTO USING (BOARD_NO)
		WHERE BOARD_NO BETWEEN 32 AND 56
	</entry>
	
	<!-- 이민주 이미지 정보 얻어오기 -->
	<entry key="selectPhoto">
		SELECT *
		FROM PHOTO
	</entry>

	<!-- 정연수 sql -->
	<!-- 팬아트 게시글 정보 얻어오기 -->
	<entry key="selectBoard">
		SELECT *
		FROM BOARD
		WHERE BOARD_NO = ?
		AND BOARD_ST = 'N'
	</entry>

	<!-- 팬아트 모든 게시글 정보 가져오기 -->
	<entry key="selectAllBoards">
		SELECT *
		FROM BOARD
	</entry>

	<!-- 팬아트 게시글/이미지 가져오기 -->
	<entry key="selectBoardWithPhotos">
		SELECT b.BOARD_NO, b.BOARD_TITLE, b.BOARD_CONTENT,
		p.CONTENT_PATH ,
		b.READ_COUNT, b.BOARD_CD
		FROM BOARD b
		LEFT JOIN PHOTO p
		ON (b.BOARD_NO = p.BOARD_NO)
		JOIN MEMBER m ON(B.MEMBER_NO =
		m.MEMBER_NO)
		WHERE BOARD_CD = 2
		AND BOARD_ST = 'N'
	</entry>

	<!-- 팬아트 게시글/이미지 가져오기(BOARD_NO)에 따라 상세 페이지 -->
	<entry key="selectBoardWithPhotos2">
		SELECT b.BOARD_NO, b.BOARD_TITLE, b.BOARD_CONTENT,
		b.CREATE_DT,
		p.CONTENT_PATH, b.READ_COUNT, m.MEMBER_NICK, b.BOARD_CD,b.MEMBER_NO
		FROM BOARD b
		LEFT JOIN PHOTO p ON (b.BOARD_NO = p.BOARD_NO)
		JOIN MEMBER m ON(B.MEMBER_NO = m.MEMBER_NO)
		WHERE b.BOARD_NO = ?
		AND BOARD_ST = 'N'

	</entry>

	<!-- 팬아트 조회수 증가 -->
	<entry key="updateViewCount">
		UPDATE BOARD
		SET READ_COUNT = READ_COUNT + 1
		WHERE
		BOARD_NO = ?
	</entry>

	<!-- 팬아트 게시판 이미지 삽입 -->
	<entry key="insertBoardImage">
		INSERT INTO PHOTO
		VALUES(?, ?,DEFAULT,DEFAULT)

	</entry>

	<!-- 팬아트 다음 게시글 번호 조회 -->
	<entry key="nextBoardNo">
		SELECT SEQ_BOARD_NO.NEXTVAL FROM DUAL
	</entry>

	<!-- 팬아트 게시판 작성하기 -->
	<entry key="insertBoard">
		INSERT INTO BOARD VALUES(?,?,?,DEFAULT,DEFAULT, DEFAULT, ?, DEFAULT,
		DEFAULT, DEFAULT, DEFAULT, DEFAULT, ?, DEFAULT)
	</entry>

	<!-- 팬아트 게시글 수정 -->
	<entry key="updateBoard">
		UPDATE BOARD SET BOARD_TITLE = ?, BOARD_CONTENT = ?
		WHERE BOARD_NO = ?
	</entry>

	<!-- 팬아트 게시글 이미지 수정 -->
	<entry key="updateBoardImage">
		UPDATE PHOTO SET
		CONTENT_PATH = ?
		WHERE BOARD_NO = ?
	</entry>

	<!-- 팬아트 게시글 이미지 삭제 -->
	<entry key="deleteBoardImage">
		DELETE FROM PHOTO
		WHERE BOARD_NO = ?
	</entry>

	<!-- 팬아트 게시글 삭제 -->
	<entry key="deleteBoard">
		UPDATE BOARD
		SET BOARD_ST = 'Y'
		WHERE BOARD_NO = ?
	</entry>
	<!-- 정연수 SQL 끝 -->



	<!-- 이동호 sql -->
	<!-- 게시글 이름 조회 -->
	<entry key="selectBoardName">
		SELECT BOARD_NM FROM BOARD_TYPE
		WHERE BOARD_CD = ?
	</entry>

	<entry key="getListCount">
		SELECT COUNT(*) FROM BOARD
		WHERE BOARD_CD = ?
		AND BOARD_ST = 'N'
	</entry>

	<!-- 게시글 목록 조회 -->
	<entry key="selectBoardList">
		SELECT * FROM(
		SELECT ROWNUM RNUM, A.* FROM(
		SELECT BOARD_NO, BOARD_TITLE, MEMBER_NICK,
		TO_CHAR(CREATE_DT, 'YYYY-MM-DD')AS CREATE_DT,
		READ_COUNT
		FROM BOARD
		JOIN MEMBER USING(MEMBER_NO)
		WHERE BOARD_CD = ?
		AND BOARD_ST = 'N'
		ORDER BY BOARD_NO DESC
		) A
		)
		WHERE RNUM BETWEEN ? AND ?
	</entry>

	<!-- 특정 조건을 만족하는 게시글 수 조회 -->
	<entry key="searchListCount">
		SELECT COUNT(*) FROM BOARD
		JOIN MEMBER USING(MEMBER_NO)
		WHERE BOARD_ST = 'N'
		AND BOARD_CD = ?
	</entry>

	<!-- 특정 게시판에서 조건을 만족하는 게시글 조회 -->
	<entry key="searchBoardList1">
		SELECT * FROM(
		SELECT ROWNUM RNUM, A.* FROM(
		SELECT BOARD_NO, BOARD_TITLE, MEMBER_NICK,
		TO_CHAR(CREATE_DT, 'YYYY-MM-DD')AS CREATE_DT, READ_COUNT
		FROM BOARD
		JOIN MEMBER USING(MEMBER_NO)
		WHERE BOARD_CD = ?
		AND BOARD_ST = 'N'
	</entry>

	<entry key="searchBoardList2">
		ORDER BY BOARD_NO DESC
		) A
		)
		WHERE RNUM BETWEEN ? AND ?
	</entry>

	<entry key="selectBoardDetail">
		SELECT BOARD_NO, BOARD_TITLE, BOARD_CONTENT, CREATE_DT, READ_COUNT,
		MEMBER_NO, MEMBER_NICK,
		PROFILE_IMG, BOARD_NM
		FROM BOARD
		JOIN MEMBER USING (MEMBER_NO)
		JOIN BOARD_TYPE USING (BOARD_CD)
		WHERE BOARD_NO = ?
	</entry>
	<!-- 이동호 sql끝 -->
	
	
	
	
	<!-- 박정흠 sql -->
	<entry key="selectThemeList">
		SELECT BOARD_TITLE, BOARD_CONTENT, CONTENT_PATH
		FROM BOARD 
		JOIN PHOTO USING(BOARD_NO)
		WHERE BOARD_CD = 1
	</entry>
	
	<!-- 굿즈조회 -->
	<entry key="selectGoodsList">
		SELECT BOARD_TITLE, CONTENT_PATH
		FROM BOARD 
		JOIN PHOTO USING(BOARD_NO)
		WHERE BOARD_CD = 3
	</entry>
	
	<!-- 테마추천 상세 내용조회 -->
	<entry key="selectBoardThemeDetail">
		SELECT BOARD_TITLE, BOARD_CONTENT, AGE, GENRE
		FROM BOARD
		WHERE BOARD_CD = 10
		AND BOARD_NO IN ( ?, ?, ?, ?, ? )
		ORDER BY BOARD_NO
	</entry>
	
	<!--  테마추천 상세 이미지조회 -->
	<entry key="selectPhotoList">
	
	</entry>
	
	<!-- 박정흠 sql 끝  -->
	


</properties>
